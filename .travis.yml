language: php

php:
  - 5.5
  - 5.6
  - 7.0

env:
  - CLOUDINARY_CLOUD_NAME=ddnvpqjmh CLOUDINARY_API_KEY=911597418222643 CLOUDINARY_API_SECRET=qgnRvc9ACfuMQjrm2dNmrKTCYqc

sudo: false

matrix:
  allow_failures:
    - php: 7.0
    - php: hhvm
before_install:
  - composer install --dev

install:
  - curl -s http://getcomposer.org/installer | php
  - php composer.phar install --dev --no-interaction
script:
  - mkdir -p build/logs
  - php vendor/bin/phpunit -c phpunit.xml
  - ./vendor/bin/phpunit --coverage-clover ./tests/logs/clover.xml

after_script:
  - php vendor/bin/coveralls -v
